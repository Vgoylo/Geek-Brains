<% if current_user %>
  <div class="container col-md-4">
    <%= link_to 'Back', root_path, class: 'btn btn-primary' %>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <th scope="col">№</th>
        <th scope="col">
          <%= 'email' %>
      </tr>
    </thead>
    <tbody class="table table-striped">
      <% @users.each_with_index do |user, index| %>
          <tr>
            <th scope="row"><%= index + 1 %></th>
            <td><%= link_to user.email, admin_users_path(user) %></td>
            <% if current_user %>
            
              <td><%= link_to 'Destroy', admin_users_path(user), method: :delete %></td>
            <% end %>
          </tr>
          <% end %>
    </tbody>
  </table>
<% end %>